/**
 * MobileReportCardsScreen — mobile view for the ReportCards module.
 * Design: PENDING — Stitch "Institutional Excellence" mobile spec.
 */
import styles from "./MobileReportCardsScreen.module.css";

export function MobileReportCardsScreen() {
  return (
    <section className={${styles.root}} aria-label="ReportCards">
      <div className={${styles.placeholder}}>
        <span className={${styles.label}}>ReportCards — Mobile Screen</span>
        <span className={${styles.hint}}>Design pending. Structure ready.</span>
      </div>
    </section>
  );
}