/**
 * admin.routes.ts — Express routes for the admin module.
 * These mirror the Next.js API routes in src/app/api/.
 * Implement each handler by importing the matching service from ../services/.
 */
import { Router } from "express";

export const adminRouter = Router();

// TODO: implement routes by importing from ../services/admin.service
// Example:
// import { listadmin.Value.ToUpper()dmin } from "../services/admin.service";
// adminRouter.get("/", async (req, res, next) => { ... });