/**
 * exams.routes.ts — Express routes for the exams module.
 * These mirror the Next.js API routes in src/app/api/.
 * Implement each handler by importing the matching service from ../services/.
 */
import { Router } from "express";

export const examsRouter = Router();

// TODO: implement routes by importing from ../services/exams.service
// Example:
// import { listexams.Value.ToUpper()xams } from "../services/exams.service";
// examsRouter.get("/", async (req, res, next) => { ... });