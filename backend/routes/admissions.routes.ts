/**
 * admissions.routes.ts — Express routes for the admissions module.
 * These mirror the Next.js API routes in src/app/api/.
 * Implement each handler by importing the matching service from ../services/.
 */
import { Router } from "express";

export const admissionsRouter = Router();

// TODO: implement routes by importing from ../services/admissions.service
// Example:
// import { listadmissions.Value.ToUpper()dmissions } from "../services/admissions.service";
// admissionsRouter.get("/", async (req, res, next) => { ... });